/*
Input: Array of elements

["h","o","l","a"]

Output: String with comma delimited elements of the array in th same order.

"h,o,l,a"
*/


//My Solution

function printArray(array){
    return array.toString();
}

/*
P: params is the the array of ["h","o","l","a"]

R: return and convert the array into a string in the same order as the array

E: "h,o,l,a"
"e,l,m"

P: 1 - use the .toString() method which will return a string representing an object. 
 */